// Named Function: Functions with a specific name. They are reusable and helpful for debugging because the function name appears in stack traces.
function greet(name) {
  return `Hello, ${name}`;
}

console.log(greet("Saidul"));
