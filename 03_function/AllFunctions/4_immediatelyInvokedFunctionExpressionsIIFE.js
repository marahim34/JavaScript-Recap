// Immediately Invoked Function Expressions (IIFE): Functions that are executed immediately after being defined. Useful for creating a private scope.
// Use IIFEs to create private scopes and avoid polluting the global namespace. They’re useful for running initialization code immediately.

(function () {
  console.log("This is the IIFE");
})();
